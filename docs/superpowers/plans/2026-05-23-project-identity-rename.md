# Project Identity Rename: base44-app → qoidrifat-portofolio

> **For agentic workers:** Use `superpowers:executing-plans` or `superpowers:subagent-driven-development` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rename the project identity from `base44-app` to `qoidrifat-portofolio` — metadata and documentation only, zero behavioral change.

**Architecture:** Three files touched, all metadata/documentation. No source code under `src/` changes. No dependency tree changes. The lockfile is regenerated (not hand-edited) to stay consistent with npm's expected format.

**Tech Stack:** Node.js / npm, Vite

---

## Out of Scope (confirmed by approach spec D4)

- `@base44/sdk`, `@base44/vite-plugin` dependencies — untouched
- `base44_*` localStorage keys in `src/lib/app-params.jsx` — untouched
- Folder name `my-portofolio` — untouched
- Firebase project ID in `.firebaserc` — untouched
- `index.html` — already correct (title and meta tags already say "Qoid Rif'at")

---

### Task 1: Update package.json name field

**Files:**
- Modify: `package.json:2`

- [ ] **Step 1: Change the `name` field**

From:
```json
{
  "name": "base44-app",
```

To:
```json
{
  "name": "qoidrifat-portofolio",
```

- [ ] **Step 2: Verify the edit**

Run: `node -e "const p = require('./package.json'); console.log(p.name)"`
Expected output: `qoidrifat-portofolio`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "refactor: rename project from base44-app to qoidrifat-portofolio in package.json"
```

---

### Task 2: Regenerate package-lock.json

**Files:**
- Modify: `package-lock.json` (regenerated by npm, not hand-edited)

- [ ] **Step 1: Run npm install to regenerate the lockfile**

Run: `npm install`
Expected: Exit code 0, no EJSONPARSE or name-mismatch warnings.

- [ ] **Step 2: Verify the lockfile diff**

Run: `git diff package-lock.json`

Verify that ONLY the two `name` fields changed:
- Root `"name": "base44-app"` → `"name": "qoidrifat-portofolio"`
- Nested `packages.""` entry: `"name": "base44-app"` → `"name": "qoidrifat-portofolio"

Verify that NO dependency versions or integrity hashes changed. If the diff shows dependency changes, the lockfile was out of sync before this rename — investigate and decide whether to accept or revert.

- [ ] **Step 3: Commit**

```bash
git add package-lock.json
git commit -m "chore: regenerate package-lock.json for project rename"
```

---

### Task 3: Rewrite README.md identity sections

**Files:**
- Modify: `README.md:1-11`

- [ ] **Step 1: Replace the identity header and About section**

The current top of README.md (lines 1-11):
```
**Welcome to your Base44 project** 

**About**

View and Edit  your app on [Base44.com](http://Base44.com) 

This project contains everything you need to run your app locally.

**Edit the code in your local development environment**

Any change pushed to the repo will also be reflected in the Base44 Builder.
```

Replace with:
```
# qoidrifat-portofolio

Personal portfolio website for Qoid Rif'at — AI & Web Developer.

Built with Vite, React, and Tailwind CSS. This project uses the Base44 SDK for backend services.

**Edit the code in your local development environment**

Any change pushed to the repo will also be reflected in the Base44 Builder.
```

Preserve everything from line 13 (`**Prerequisites:**`) onward unchanged — the Base44 platform setup instructions, env var block, `npm run dev` instruction, "Publish your changes", and "Docs & Support" sections stay exactly as-is.

- [ ] **Step 2: Verify README reads coherently end-to-end**

Read through the full README.md and confirm:
- Top section introduces the project as a personal portfolio (qoidrifat-portofolio)
- Prerequisites, env-var block (`VITE_BASE44_APP_ID`, `VITE_BASE44_APP_BASE_URL`), `npm run dev`, Publish, and Docs & Support sections are all still present and correct

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: update README identity for qoidrifat-portofolio portfolio"
```

---

### Task 4: Final integration verification

- [ ] **Step 1: Clean install check**

Run: `npm install`
Expected: Exit code 0, no warnings about name mismatch.

- [ ] **Step 2: Dev server smoke test**

Run: `npm run dev`
Expected: Vite dev server starts without errors or name-related warnings. Kill the server after confirming (Ctrl+C).

- [ ] **Step 3: Review the full diff**

Run: `git diff main...HEAD`

Confirm:
- Only `package.json`, `package-lock.json`, and `README.md` are changed
- No source files under `src/` appear in the diff
- No dependency versions changed in the lockfile
- No config files (Vite, Tailwind, ESLint, PostCSS, Firebase, Wrangler) appear in the diff

---

## Verification Checklist (from approach spec)

1. [x] `npm install` exits 0, no EJSONPARSE or name-mismatch warnings
2. [x] `npm run dev` starts the Vite dev server, app loads at the dev URL
3. [x] `package.json` and `package-lock.json` diffs show only `name` fields changed; dependency entries untouched
4. [x] `README.md` reads end-to-end: portfolio identity at top, Base44 env-var/publish instructions still present and correct
